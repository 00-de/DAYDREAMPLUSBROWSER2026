# グループ共有の設定と使い方

5人でメンバー・ライブ・曲・連絡先などを共有できます。

---

## 1. Firestore のルールを更新する（最初に必ず）

グループを使うには、Firebase 側のルール変更が必要です。
**これをしないとグループが作れません。**

1. https://console.firebase.google.com でプロジェクトを開く
2. 左メニュー **構築 → Firestore Database**
3. 上部の **ルール** タブ
4. 中身を丸ごと下の内容に置き換えて **公開**

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 個人のデータ。本人だけが読み書きできます。
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // グループ本体
    match /groups/{groupId} {

      // 参加している人は読めます
      allow read: if request.auth != null;

      // 作成できるのは、自分を作成者として登録する場合のみ
      allow create: if request.auth != null
                    && request.resource.data.ownerUid == request.auth.uid
                    && request.auth.uid in request.resource.data.memberUids;

      // 更新できるのは、参加している人
      // または、これから参加しようとしている人
      allow update: if request.auth != null
                    && (request.auth.uid in resource.data.memberUids
                        || request.auth.uid in request.resource.data.memberUids);

      // 削除できるのは作成者だけ
      allow delete: if request.auth != null
                    && resource.data.ownerUid == request.auth.uid;

      // グループの中身（メンバー・ライブ・曲など）
      match /daydream/{document=**} {
        allow read, write: if request.auth != null
                           && request.auth.uid in
                              get(/databases/$(database)/documents/groups/$(groupId)).data.memberUids;
      }
    }

  }
}
```

このルールの意味は次のとおりです。

- 個人のデータは、本人だけが読み書きできる
- グループの中身は、**参加している人だけ**が読み書きできる
- グループから外された人は、その時点で見られなくなる

---

## 2. グループを作る（代表者だけ）

1. アプリを起動して **アカウント** タブを開く
2. ログインする
3. 「グループで共有」の枠で **グループを作る**
4. グループ名と、自分の表示名を入力
5. **作成する**

作成すると、**6文字の合言葉**が表示されます。

---

## 3. 仲間を招く

表示された合言葉を、参加してほしい人に伝えてください。

```
合言葉： K7M2XP
```

LINE や口頭で構いません。

---

## 4. 参加する（招かれた人）

1. アプリをインストールして起動
2. **アカウント** タブでログイン（各自のメールアドレスで新規作成）
3. 「グループで共有」の枠で **合言葉で参加する**
4. 合言葉と、自分の表示名を入力
5. **参加する**

これで、代表者と同じ内容が見えるようになります。

---

## 5. すでに入力した内容を移す

代表者が一人で入力していた内容がある場合、
**自分の内容をグループへ移す** ボタンで一括で移せます。

移されるのは次の内容です。

- メンバー、ライブ予定、曲、MV、SNS、目標
- メールの連絡先、ひな形、送信済み、配信ログ

> ⚠️ グループ側に同じ種類の内容があると、上書きされます。
> 誰かが先に入力していないか、確認してから実行してください。

---

## 使い方の切り替え

「グループの内容を表示中／自分だけの内容を表示中」の
スイッチで、いつでも切り替えられます。

| 状態 | 内容 |
|---|---|
| グループの内容 | 全員で共有。誰かが編集すると全員に反映 |
| 自分だけの内容 | 自分専用。他の人には見えません |

下書きや個人的なメモは、自分だけの内容側で扱うと便利です。

---

## 共有されるもの・されないもの

| 共有される | 共有されない |
|---|---|
| メンバー情報 | 背景の設定 |
| ライブ予定 | Dock の並び |
| 曲・MV | お気に入り |
| SNS・目標 | 開いているタブ |
| メールの連絡先 | Workspace の配置 |
| メールのひな形 | ログイン情報 |
| 送信済み・配信ログ | |

見た目や作業状態は、各自の好みで設定できます。

---

## メンバーの管理

**グループを作った人**だけができること。

- メンバーを外す（各行の ✕ ボタン）
- 合言葉を作り直す（漏れたときに使います）

外された人は、その時点でグループの内容が見えなくなります。

---

## 注意点

**合言葉を知っていれば誰でも参加できます。**
外部に漏れた場合は、「合言葉を作り直す」で無効にしてください。
すでに参加している人には影響しません。

**同時に編集すると、後から保存したほうが残ります。**
同じ項目を二人が同時に書き換えると、片方の内容が消えます。
「いま誰が何を触っているか」を、ひとこと声を掛け合うと安全です。

**参加できるのは、一人につき一つのグループまでです。**
別のグループに移りたい場合は、先に「グループから抜ける」を行ってください。

---

## トラブル対処

| 症状 | 原因と対処 |
|---|---|
| 「作成に失敗しました」 | 手順1のルール更新が未完了 |
| 「合言葉のグループが見つかりません」 | 合言葉の打ち間違い。大文字小文字は区別しません |
| 参加したのに内容が出ない | スイッチが「自分だけの内容」になっていないか確認 |
| 相手の変更が反映されない | アプリを再起動してみてください |
