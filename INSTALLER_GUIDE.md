# インストーラー作成 手順

配布用の `.exe` を作ります。

---

## 1. アイコンを配置する

`build` という名前のフォルダをプロジェクト直下に作り、その中に `icon.ico` を入れてください。

```
DAYDREAMPLUSBROWSER2026/
├── build/
│   ├── icon.ico     ← これ
│   └── icon.png     ← 予備（任意）
├── src/
├── electron/
└── ...
```

> `0_フォルダ整理.bat` は build フォルダを扱いません。手動で作ってください。

---

## 2. 作成する

**`3_インストーラー作成.bat` をダブルクリック。**

処理は3段階です。

1. コードの検査
2. 本番用のビルド
3. インストーラーの作成

初回は Electron 本体（約110MB）をダウンロードするため、**10分ほどかかります**。2回目以降は3分程度です。

---

## 3. できあがるもの

`release` フォルダの中に、次のものができます。

| ファイル | 内容 |
|---|---|
| `DayDreamBrowser-Setup-1.0.0.exe` | **配布用のインストーラー**（これを配ります） |
| `win-unpacked/` | インストール前の中身（配布不要） |
| `builder-debug.yml` | 作成時の記録（配布不要） |

インストーラーの大きさは **80MB前後**になります。Electron 本体を含むためで、これ以上は小さくできません。

---

## 4. インストールされる内容

- インストール先を選べます（既定はユーザーフォルダ内）
- デスクトップにショートカットができます
- スタートメニューに登録されます
- 管理者権限は不要です

アンインストールは Windows の「アプリと機能」から行えます。

---

## 5. 動作を確認する

作成したインストーラーをダブルクリックしてインストールし、次を確認してください。

- [ ] デスクトップのショートカットから起動する
- [ ] ホーム画面が表示される
- [ ] 起動チェックが 6/7 以上（Firebase を設定済みなら 7/7）
- [ ] ブラウザタブで Claude や GitHub が開く
- [ ] Workspace でウィンドウが動かせる
- [ ] Dock が表示される
- [ ] DayDream タブでメンバーが見える

問題があれば、開発モード（`2_開発起動.bat`）に戻って修正してください。

---

## よくある質問

**Q. 「Windows によって PC が保護されました」と出ます**

A. 署名のないアプリでは必ず出ます。**詳細情報 → 実行** で進めます。

これを消すにはコードサイニング証明書が必要で、年間 3〜5万円ほどかかります。個人配布であれば、この警告のまま配る例が一般的です。配布相手には事前に伝えておくとよいでしょう。

**Q. ウイルス対策ソフトに止められます**

A. 署名がないため、誤検知されることがあります。Windows Defender の場合は「詳細情報 → 実行」で進めます。それでも止まる場合は、除外設定が必要です。

**Q. 他の人の PC でも動きますか**

A. **Windows 10 / 11 の 64bit 版**であれば動きます。Node.js のインストールは不要です。

**Q. 32bit 版や Mac 版は作れますか**

A. 32bit は `package.json` の `arch` に `ia32` を足せば作れます。Mac 版は Mac 実機が必要になります。

**Q. バージョンを上げたいときは**

A. `package.json` の `"version": "1.0.0"` を書き換えてから、もう一度 `3_インストーラー作成.bat` を実行してください。ファイル名も自動で変わります。

---

## 配布するときの注意

インストーラーには **Firebase の設定情報が含まれます**。

この情報（apiKey など）は公開されても問題ない値ですが、安全性は Firestore・Storage のルールで守られています。ルールを設定していない状態で配布すると、誰でもデータを読み書きできてしまいます。

配布前に、**FIREBASE_SETUP.md の手順3（ルール設定）が済んでいるか**を必ず確認してください。

---

## 技術的な補足

パッケージには、次の9ファイルだけが含まれます。

```
package.json
electron/main.cjs
electron/preload.cjs
dist/index.html
dist/assets/index-*.js
dist/assets/index-*.css
```

React や Firebase のライブラリは、ビルド時に `dist/assets` へまとめられるため、`node_modules` を同梱する必要がありません。これにより、アプリ部分は **約740KB** に収まっています。
