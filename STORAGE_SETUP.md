# メンバー写真の設定手順（Firebase Storage）

写真機能を使うには、Firebase コンソールでの準備が必要です。

---

## 先にお伝えしておくこと

Firebase Storage は、**無料枠を使う場合でもクレジットカードの登録が必要**です（2024年10月以降の新規プロジェクト）。

無料枠は 5GB・1日あたり 1GB のダウンロードまでで、**メンバー6人の顔写真では到底届きません**（1枚あたり約50KB に縮小されるため、6人で 0.3MB 程度）。実質、料金は発生しない見込みです。

とはいえカード登録に抵抗がある場合は、この機能は使わず、これまでどおり頭文字の丸表示のままでお使いいただけます。

---

## 1. Storage を有効にする

1. https://console.firebase.google.com でプロジェクトを開く
2. 左メニュー **構築 → Storage**
3. **始める** をクリック
4. カード登録を求められたら、画面の指示に従って **Blaze プラン**（従量制）へ切り替え
5. ロケーションは **asia-northeast1（東京）** を選択

> Blaze プランでも、無料枠を超えなければ請求は発生しません。
> 心配な場合は、後述の「予算アラート」を設定しておくと安心です。

---

## 2. ルールを設定する

Storage 画面の上部 **ルール** タブを開き、中身を丸ごと下の内容に置き換えて **公開** を押してください。

```
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // 自分のフォルダだけ読み書きできます
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null
                         && request.auth.uid == userId
                         && request.resource.size < 10 * 1024 * 1024;
    }

  }
}
```

このルールの意味は次のとおりです。

- ログインした本人だけが、自分のフォルダを読み書きできる
- 他人の写真は見られない
- 10MB を超えるファイルは受け付けない

---

## 3. 予算アラートを設定する（推奨）

万一の課金に備えて、上限の通知を設定しておくと安心です。

1. Firebase コンソール左下の **⚙️ → 使用量と請求額**
2. **詳細と設定** タブ
3. **予算とアラートを変更** → Google Cloud の画面が開く
4. 予算額を **1,000円** などに設定し、超えたらメールが届くようにする

---

## 4. アプリで使う

1. `2_開発起動.bat` で起動
2. **アカウント** タブでログイン（未ログインだと写真は使えません）
3. **DayDream** タブ → **メンバー**
4. 顔写真の丸を **クリック**、または画像を **ドラッグ＆ドロップ**

登録すると、丸い枠に写真が表示されます。

---

## 写真の扱いについて

| 項目 | 内容 |
|---|---|
| 対応形式 | JPEG・PNG・WebP・GIF |
| 上限 | 10MB（アップロード前） |
| 保存時 | **512×512 の JPEG に自動縮小**（約50KB） |
| 切り抜き | 中央を正方形に自動で切り抜き |
| 保存先 | `users/{あなたのID}/members/{メンバーID}.jpg` |

縦長・横長の写真は、中央を基準に正方形へ切り抜かれます。**顔が中央に来ている写真**を選ぶときれいに収まります。

---

## 写真を消したいとき

顔写真にマウスを乗せると、右上に **✕** が出ます。これを押すと写真が削除され、頭文字の表示に戻ります。

---

## トラブル対処

| 症状 | 原因と対処 |
|---|---|
| `写真を使うにはログインが必要です` | アカウントタブでログインしてください |
| `保存が許可されていません` | 手順2のルールが未設定 |
| `アップロードに失敗しました` | 手順1の Storage 有効化が未完了 |
| 写真が表示されない | 一度アプリを再起動してみてください |
| ファイルが大きすぎます | 10MB 以下の画像を選んでください |

---

## 補足

写真の URL は Firestore に保存されるため、**別の PC でログインすれば同じ写真が表示されます**。
アカウントタブの「クラウドから取り込む」は不要で、自動で反映されます。
