# Firebase 設定手順（8月5日分）

コードは完成しています。あとは Firebase コンソール側で2つ有効にするだけです。

---

## 1. Authentication を有効にする

1. https://console.firebase.google.com でプロジェクトを開く
2. 左メニュー **構築 → Authentication**
3. **始める** をクリック
4. **ネイティブプロバイダ** の中の **メール / パスワード** を選択
5. 一番上の **有効にする** をオンにする
6. **保存**

> 「メールリンク（パスワードなしでログイン）」はオフのままで構いません。

---

## 2. Firestore を作る

1. 左メニュー **構築 → Firestore Database**
2. **データベースの作成**
3. ロケーションは **asia-northeast1（東京）** を選択
4. モードは **本番環境モード** を選ぶ（テストモードは30日で読み書きできなくなります）
5. 作成

---

## 3. Firestore のルールを設定する

作ったあと、上部の **ルール** タブを開き、中身を丸ごと下の内容に置き換えて **公開** を押してください。

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 自分のデータだけ読み書きできます
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

  }
}
```

> このルールは「ログインした本人だけが、自分のデータを読み書きできる」という意味です。
> 他人のデータは一切見えません。

---

## 4. アプリで確認する

1. `2_開発起動.bat` で起動
2. 上部タブの **アカウント** を開く
3. **「アカウントをお持ちでない方はこちら」** を押す
4. メールアドレスとパスワード（6文字以上）を入力して **アカウントを作成**

作成に成功すると、そのままログイン状態になります。

---

## 5. 同期を試す

1. ホーム画面でお気に入りを何か変更する
2. **アカウント** タブへ戻る
3. **↑ この PC の設定を保存** を押す
4. 「クラウドへ保存しました。」と出れば成功

別の PC で同じアカウントにログインし、**↓ クラウドから取り込む** を押すと、同じ設定になります。

---

## 同期される内容

- お気に入り
- Dock の並び
- 保存したレイアウト

タブの開閉状況や、Workspace で開いているウィンドウは同期されません（PCごとの作業状態のため）。

---

## トラブル対処

| 症状 | 原因と対処 |
|---|---|
| `メール認証が有効になっていません` | 手順1が未完了 |
| `保存に失敗しました` | 手順3のルールが未設定。ルールタブを確認 |
| `クラウドの読み取りに失敗しました` | 同上 |
| `パスワードは6文字以上に` | そのまま。6文字以上で作り直してください |
| アカウントタブが「Firebase が未設定です」 | `src/lib/firebase.ts` の設定を確認 |

---

## セキュリティについて

`firebase.ts` に書く設定情報（apiKey など）は、**公開されても問題ない値**です。
実際の安全性は、手順3の Firestore ルールで守られています。

ただし GitHub リポジトリは **Private のまま**にしておくことをおすすめします。
