# インストーラー作成 実行手順

## 事前確認

`3_インストーラー作成.bat` を実行する前に、次を確認してください。

- [ ] `build` フォルダがあり、中に `icon.ico` が入っている
- [ ] `2_開発起動.bat` でアプリが正常に起動する
- [ ] 上部タブが8つある（ホーム／ブラウザ／Workspace／DayDream／メール／見た目／アカウント／起動チェック）

---

## 実行

**`3_インストーラー作成.bat` をダブルクリック。**

処理は3段階で進みます。

```
[1/3] コードを検査しています...
[2/3] 本番用にビルドしています...
[3/3] インストーラーを作成しています...
```

初回は Electron 本体（約110MB）をダウンロードするため、**10分ほど**かかります。
2回目以降は3分程度です。

完了すると `release` フォルダが自動で開きます。

---

## できあがるもの

| ファイル | 用途 |
|---|---|
| `DayDreamBrowser-Setup-1.0.0.exe` | **配布用**（これを配ります・約80MB） |
| `win-unpacked/` | 作業用（配布不要） |
| `builder-debug.yml` | 記録（配布不要） |

---

## インストールして確認する

`DayDreamBrowser-Setup-1.0.0.exe` をダブルクリックします。

### 「Windows によって PC が保護されました」が出たら

署名のないアプリでは必ず出ます。**詳細情報 → 実行** で進めてください。

これを消すには、年間3〜5万円のコードサイニング証明書が必要です。
個人配布では、この警告のまま配る例が一般的です。

### 動作確認

インストール後、デスクトップのショートカットから起動し、次を確認してください。

- [ ] ウィンドウが開く
- [ ] タブが8つ表示される
- [ ] ホーム画面に時計とメンバーの挨拶が出る
- [ ] ブラウザタブで Claude や GitHub が開く
- [ ] Workspace でウィンドウを動かせる
- [ ] Dock が画面下に出る
- [ ] DayDream タブにメンバー6人が並ぶ
- [ ] メールタブが開く
- [ ] 見た目タブで背景を変えられる
- [ ] アカウントタブでログインできる
- [ ] 起動チェックが 7/7（Firebase 設定済みの場合）

---

## 検証済みの内容

このプロジェクトは、次の検証を通過しています。

| 項目 | 結果 |
|---|---|
| ESLint | 警告 0 |
| TypeScript 型チェック | エラー 0 |
| 本番ビルド | 成功（751KB / gzip 195KB） |
| パッケージング | 成功（9ファイル / 791KB） |
| 本番モードでの起動 | 成功・クラッシュなし |
| 画面の描画 | 8タブすべて表示を確認 |
| preload の橋渡し | 正常動作 |
| JavaScript エラー | なし |

---

## うまくいかないとき

| 症状 | 対処 |
|---|---|
| `アイコンが見つかりません` | `build\icon.ico` を配置してください |
| `[1/3] で止まる` | コードに問題があります。表示内容をご確認ください |
| `electron-builder` でエラー | ウイルス対策ソフトが妨げている可能性。一時的に無効化してお試しください |
| 途中で止まる | インターネット接続をご確認ください |

---

## バージョンを上げるとき

`package.json` の 3行目

```
"version": "1.0.0",
```

を書き換えてから、もう一度 `3_インストーラー作成.bat` を実行してください。
ファイル名も自動で変わります。

---

## 配布前の確認事項

インストーラーには **Firebase の設定情報が含まれます**。

この情報自体は公開されても問題ありませんが、安全性は Firestore と Storage の
**ルール設定**によって守られています。

配布前に、`FIREBASE_SETUP.md` の手順3（ルール設定）が完了しているか、
必ずご確認ください。ルール未設定のまま配布すると、誰でもデータを
読み書きできてしまいます。
