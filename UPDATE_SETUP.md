# 自動更新の設定と使い方

新しいバージョンを公開すると、利用者のアプリが自動で気づいて更新します。

---

## 1. package.json を書き換える（最初だけ）

`package.json` の下のほうにある `publish` の部分を、ご自身のものに書き換えてください。

```json
"publish": [
  {
    "provider": "github",
    "owner": "ここにGitHubのユーザー名",
    "repo": "ここにリポジトリ名",
    "releaseType": "release"
  }
]
```

**確認のしかた**

GitHub でリポジトリを開いたときの URL を見てください。

```
https://github.com/tosi-daydream/DAYDREAMPLUSBROWSER2026
                   ~~~~~~~~~~~~~  ~~~~~~~~~~~~~~~~~~~~~~
                   owner          repo
```

この2つを書き写します。

> ⚠️ リポジトリは **Public** である必要があります。
> Private の場合、更新情報を取得できません。

---

## 2. 新しいバージョンを公開する手順

### ① バージョン番号を上げる

`package.json` の3行目を書き換えます。

```json
"version": "1.0.1",
```

数字の意味は次のとおりです。

| 例 | 使いどころ |
|---|---|
| 1.0.0 → 1.0.1 | 小さな修正 |
| 1.0.1 → 1.1.0 | 機能の追加 |
| 1.1.0 → 2.0.0 | 大きな作り直し |

**必ず前より大きい数字にしてください。** 同じか小さいと、更新と認識されません。

### ② インストーラーを作る

`3_インストーラー作成.bat` をダブルクリック。

`release` フォルダに、次の2つができます。

| ファイル | 役割 |
|---|---|
| `DayDreamBrowser-Setup-1.0.1.exe` | インストーラー本体 |
| `latest.yml` | **更新情報（これも必ず必要）** |

> `latest.yml` を忘れると、更新が見つかりません。必ず両方を公開してください。

### ③ GitHub で公開する

1. GitHub でリポジトリを開く
2. 右側の **Releases** → **Create a new release**
3. **Choose a tag** → `v1.0.1` と入力 → **Create new tag**
   （バージョン番号の前に `v` を付けます）
4. **Release title** に `v1.0.1` と入力
5. **Describe this release** に変更内容を書く（利用者に表示されます）
6. 下の枠に、`release` フォルダから**2つのファイルをドラッグ＆ドロップ**
   - `DayDreamBrowser-Setup-1.0.1.exe`
   - `latest.yml`
7. **Publish release** をクリック

### ④ 完了

利用者がアプリを起動すると、**8秒後に自動で確認**し、更新があれば知らせます。

---

## 利用者側の見え方

**アカウント** タブの下に「アプリの更新」という枠があります。

| 状態 | 表示 |
|---|---|
| 最新 | 「最新の状態です」 |
| 更新あり | 「新しいバージョン 1.0.1 が公開されています」＋ ダウンロードボタン |
| ダウンロード中 | 進み具合のバーと速度 |
| 完了 | 「再起動して適用する」ボタン |

「今すぐ確認する」ボタンで、いつでも手動で調べられます。

ダウンロードは**自動では始まりません**。利用者がボタンを押したときだけ行われます。

---

## 更新の適用について

- **「再起動して適用する」** を押すと、すぐに更新されます
- 押さなかった場合も、**次にアプリを終了したときに自動で適用**されます

---

## 動作確認済みの内容

| 検証項目 | 結果 |
|---|---|
| electron-updater のパッケージ同梱 | 確認済み |
| `window.dd.update` の公開 | 5つのメソッドすべて動作 |
| バージョン取得 | 正常 |
| 状態通知の購読・解除 | 正常 |
| 更新確認の実行 | 正常 |
| 本番ビルドでの起動 | クラッシュなし |

---

## トラブル対処

| 症状 | 原因と対処 |
|---|---|
| 「更新情報が見つかりませんでした」 | Releases に `latest.yml` が無い。または publish の owner / repo が違う |
| 「権限がありません」 | リポジトリが Private のまま。Public にしてください |
| 「開発モードでは確認しません」 | 正常です。インストール版でのみ動作します |
| 更新に気づかない | バージョン番号が前回以下になっていないか確認 |
| ダウンロードが進まない | インターネット接続を確認 |

---

## 注意点

**署名がないため、更新時にも Windows の警告が出ることがあります。**
これは通常のインストールと同じで、避けられません。

**`release` フォルダは GitHub に上げないでください。**
`.gitignore` で除外していますが、もし変更一覧に出てきたら、
チェックを外すかフォルダを削除してください。
100MB を超えるファイルは GitHub が受け付けません。

**リポジトリを Public にすると、Firebase の設定情報が公開されます。**
これ自体は公開前提の値ですが、**Firestore のルール設定が必須**です。
`FIREBASE_SETUP.md` の手順3が済んでいるか、必ずご確認ください。
