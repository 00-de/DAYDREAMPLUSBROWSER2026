# 自動公開の設定と使い方

ボタン2つで、友人のアプリまで更新が届くようになります。

---

## 全体の流れ

```
8_バージョンを上げる.bat をダブルクリック
        ↓
7_公開.bat をダブルクリック
        ↓
ブラウザで「Publish release」を押す
        ↓
友人のアプリに更新のお知らせが届く
```

これまで手作業だった「インストーラーの受け渡し」「ファイルのアップロード」は不要になります。

---

## 1. トークンを発行する（最初だけ）

GitHub に自動でアップロードするための合鍵です。

**`9_トークン登録.bat` をダブルクリック**すると、案内に沿って進められます。

手動で行う場合は、次のとおりです。

1. https://github.com/settings/tokens/new を開く
2. **Note** に `DayDream Browser` と入力
3. **Expiration** は `No expiration`（期限なし）を選ぶ
4. **Select scopes** の **repo** にチェックを入れる
5. 一番下の **Generate token** を押す
6. 表示された `ghp_` で始まる文字列をコピー

> ⚠️ この文字列は**一度しか表示されません**。
> 閉じる前にコピーしてください。

---

## 2. トークンを登録する（最初だけ）

`9_トークン登録.bat` の案内に従って貼り付けるだけです。

登録が終わったら、**黒い画面を一度閉じてください。**
閉じないと、次に開く画面に反映されません。

### 手動で登録する場合

1. Windows の検索で「環境変数」と入力
2. 「システム環境変数の編集」を開く
3. 「環境変数」ボタン
4. 上段（ユーザー環境変数）の「新規」
5. 変数名 `GH_TOKEN`、変数値にコピーした文字列
6. OK で閉じる

---

## 3. 公開する（毎回）

### ① バージョンを上げる

**`8_バージョンを上げる.bat` をダブルクリック。**

3つから選ぶだけです。

| 選択 | 例 | 使いどころ |
|---|---|---|
| 1 小さな修正 | 1.0.0 → 1.0.1 | 不具合を直した |
| 2 機能を足した | 1.0.1 → 1.1.0 | 新しい画面を追加した |
| 3 大きく作り直した | 1.1.0 → 2.0.0 | 全面的に変えた |

迷ったら **1** で構いません。

### ② 公開する

**`7_公開.bat` をダブルクリック。**

検査 → ビルド → インストーラー作成 → GitHub へアップロード、まで自動で進みます。
5分ほどかかります。

終わるとブラウザが自動で開きます。

### ③ ブラウザで仕上げ

アップロードされたものは、**下書きの状態**で置かれています。
中身を確かめてから公開する仕組みです。

1. 一番上の `v1.0.1` を開く
2. 右上の**鉛筆マーク**を押す
3. **変更内容を書く**（利用者の画面に表示されます）
4. **Set as the latest release** にチェックが入っているか確認
5. **Publish release** を押す

これで完了です。

---

## 友人側で起きること

アプリを起動すると、8秒後に自動で確認が走ります。

更新があれば、アカウントタブに

> 新しいバージョン 1.0.1 が公開されています

と表示され、ボタンを押すだけで最新版になります。

**ファイルを渡す必要はありません。**

---

## 書く内容の例

変更内容の欄には、こういったことを書きます。

```
・ツールをアプリ内から追加できるようになりました
・背景に明るい色を追加しました
・グループでデータを共有できるようになりました
```

難しく書く必要はありません。何が変わったか伝われば十分です。

---

## 検証済みの内容

| 項目 | 結果 |
|---|---|
| バージョン計算（6パターン） | 6/6 合格 |
| 文字コード（Shift-JIS） | 正常 |
| 改行コード（CRLF） | 正常 |
| トークン未登録時の案内 | 動作 |

---

## トラブル対処

| 症状 | 原因と対処 |
|---|---|
| 「トークンが登録されていません」 | `9_トークン登録.bat` を実行。その後、画面を開き直す |
| 「同じバージョンが既にあります」 | `8_バージョンを上げる.bat` で数字を上げる |
| 401 や 403 のエラー | トークンの期限切れ、または repo の権限がない |
| アップロードが途中で止まる | 通信の状態を確認して、もう一度実行 |
| 友人に通知が届かない | 「Set as the latest release」のチェックを確認 |

---

## 安全のために

**トークンは、他人に渡ると危険です。**
リポジトリを書き換えられてしまう可能性があります。

- 画面共有や配信では、`9_トークン登録.bat` の画面を映さない
- トークンをメモ帳などに残さない
- 万一漏れたら、GitHub の Settings → Tokens から削除する

環境変数はこの PC の中だけに保存され、配布するアプリには含まれません。

---

## 従来の方法も残っています

`3_インストーラー作成.bat` は、これまでどおり使えます。
GitHub にアップロードせず、インストーラーだけ作りたい場合にお使いください。
